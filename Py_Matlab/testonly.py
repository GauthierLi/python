# ---------------------------------------------------------------------------------------------------------------------
# ---------------------------------------------------- 开发块----------------------------------------------------------
# ---------------------------------------------------------------------------------------------------------------------

# ---------------------------------------------------------------------------------------------------------------------
# ------------------------------------------------------调试块---------------------------------------------------------
# ---------------------------------------------------------------------------------------------------------------------


# N = Mat([[5, 0], [7, 1], [6, 4]])

# M = Polynomial([[5,1], [2,0]])
# N = Polynomial([[1,0], [4,1]])

import Py_Mat as pm
a = pm.Mat([[0]*8 for i in range(8)])
for i in range(8):
    for j in range(8):
        a[i][j] = (j+1)**i
pm.Mat_Print(pm.Mat_Inverse(a))


# import Py_Mat as pm
# print(pm.Float(1.001*10))

import numpy as np
a = np.array([[0]*8 for i in range(8)])
for i in range(8):
    for j in range(8):
        a[i][j] = (j+1)**i
print(np.linalg.inv(a))